[pytest]
# 测试文件发现规则
testpaths = tests/
python_files = test_*.py
python_classes = Test*

# 默认运行参数（每次跑用例自动带上）
addopts =
    -v
    -s
    --tb=short
    --browser=edge

# 日志级别
log_cli = true
log_cli_level = INFO
